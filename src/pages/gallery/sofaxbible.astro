---
import type { ImageMetadata } from "astro";
import GalleryLayout from "../../layouts/GalleryLayout.astro";

var images = import.meta.glob<{ default: ImageMetadata }>("../../../public/image/sofaxbible/*");
const fontColor = "#f74376";
const backgroundColor = "#FBE7E6";
const albumName = "Sofa & Bible";
---

<GalleryLayout title={albumName} backgroundColor={backgroundColor} fontColor={fontColor} imageData={images} />
